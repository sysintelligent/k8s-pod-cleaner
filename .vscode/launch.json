{
    "configurations": [
        {
            "name": "Debug k8s-pod-cleaner operator",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "args": [
                "-kubeconfig=/home/dgu/.kube/config"
            ]
        }
    ]
}